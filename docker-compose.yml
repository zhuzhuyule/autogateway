services:
  autogateway:
    # 默认拉本项目预 build 的镜像 (multi-arch: linux/amd64 + linux/arm64).
    # docker compose pull && docker compose up -d 即可滚动升级.
    image: ghcr.io/zhuzhuyule/autogateway:latest
    # 也可改用上游 GPT-Load 官方镜像(不含本项目的免费 Provider/系统聚合等增强):
    # image: ghcr.io/tbphp/gpt-load:latest
    # 想从源码本地 build 时, 把上面 image 行注释掉, 启用以下 build 块:
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    container_name: autogateway
    ports:
      - "${PORT:-3001}:${PORT:-3001}"
    env_file:
      - .env
    restart: always
    volumes:
      - ./data:/app/data
    stop_grace_period: ${SERVER_GRACEFUL_SHUTDOWN_TIMEOUT:-10}s
    healthcheck:
      test: wget -q --spider -T 10 -O /dev/null http://localhost:${PORT:-3001}/health
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    # depends_on:
    #   mysql:
    #     condition: service_healthy
    #     restart: true
    #   postgres:
    #     condition: service_healthy
    #     restart: true
    #   redis:
    #     condition: service_healthy
    #     restart: true

  # 如果需要安装 MySQL、PostgreSQL 或 Redis，请取消以下注释并配置相应的环境变量。
  # 并且要在上方的depends_on中取消注释相应的依赖服务。

  # mysql:
  #   image: mysql:8.2
  #   container_name: autogateway-mysql
  #   restart: always
  #   environment:
  #     MYSQL_ROOT_PASSWORD: 123456
  #     MYSQL_DATABASE: autogateway
  #   volumes:
  #     - ./data/mysql:/var/lib/mysql
  #   healthcheck:
  #     test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 10

  # postgres:
  #   image: "postgres:16"
  #   container_name: autogateway-postgres
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: 123456
  #     POSTGRES_DB: autogateway
  #   volumes:
  #     - ./data/postgres:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres -d autogateway"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 10

  # redis:
  #   image: redis:latest
  #   container_name: autogateway-redis
  #   restart: always
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "ping"]
  #     interval: 5s
  #     timeout: 3s
  #     retries: 3
